function Header(props) {
    return (
      <div className="Header">
        <header className="App-header">
          <h1>{props.name}</h1>
        </header>
      </div>
    );
  }
  
  export default Header;
  